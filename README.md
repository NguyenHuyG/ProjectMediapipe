Các thư viện có trong script cần tải
OpenCv (Camera)      -- pip install opencv-python
Mediapipe (AI)       -- pip install mediapipe
Pygame (Sound)       -- pip install pygame
Câu lệnh tải tất cả
pip install opencv-python mediapipe pygame
